{
  "name": "kokoro-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "http-server -p 8080 -c-1 .",
    "build": "rm -rf dist && mkdir -p dist && cp index.html terms.html pitch.html colors_and_type.css dist/ && cp -R assets dist/assets && cp -R public/. dist/"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
