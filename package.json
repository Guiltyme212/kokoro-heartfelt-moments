{
  "name": "kokoro-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080",
    "build": "rm -rf dist && mkdir -p dist && cp index.html terms.html privacy.html help.html pitch.html start.html colors_and_type.css dist/ && cp -R assets dist/assets && cp -R public/. dist/"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
