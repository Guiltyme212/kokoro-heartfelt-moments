# OS junk
.DS_Store
Thumbs.db

# Editor junk
.vscode/
.idea/
*.swp

# Node (if anyone adds tooling later)
node_modules/
npm-debug.log

# Local server stuff
.cache/

# Build output — regenerated by `bun run build`, never committed
dist/

# ─────────────────────────────────────────────────────────────────────
# RESEARCH OPERATION — LOCAL ONLY, NEVER COMMITTED
# Competitor analysis, screenshots, and multi-agent research output all
# live under research/. This keeps non-website artifacts out of the repo
# (and out of Lovable's sync). See CLAUDE.md → "Research operation".
# ─────────────────────────────────────────────────────────────────────
research/

# Agent / skills / planning tooling — local only, not part of the website
.claude/
.agents/
.planning/
skills-lock.json
